-- Criação do banco de dados
CREATE DATABASE tais_croches;

-- Uso do banco de dados
USE tais_croches;

-- Criação da tabela de produtos
CREATE TABLE produtos (
    id_produto INT AUTO_INCREMENT PRIMARY KEY,
    nome_produto VARCHAR(255) NOT NULL,
    preco DECIMAL(10, 2) NOT NULL,
    imagem VARCHAR(255) NOT NULL,
    estoque INT NOT NULL DEFAULT 0
);

-- Criação da tabela de usuários
CREATE TABLE usuarios (
    id_usuario INT AUTO_INCREMENT PRIMARY KEY,
    nome_usuario VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    senha VARCHAR(255) NOT NULL
);

-- Criação da tabela de pedidos
CREATE TABLE pedidos (
    id_pedido INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT,
    data_pedido TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (id_usuario) REFERENCES usuarios(id_usuario)
);

-- Criação da tabela de itens de pedido
CREATE TABLE itens_pedido (
    id_item_pedido INT AUTO_INCREMENT PRIMARY KEY,
    id_pedido INT,
    id_produto INT,
    quantidade INT NOT NULL,
    preco DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (id_pedido) REFERENCES pedidos(id_pedido),
    FOREIGN KEY (id_produto) REFERENCES produtos(id_produto)
);

ALTER TABLE usuarios
ADD COLUMN telefone VARCHAR(15) AFTER nome_usuario,
ADD COLUMN cpf VARCHAR(14) UNIQUE AFTER telefone,
MODIFY COLUMN nome_usuario VARCHAR(255) NOT NULL;

ALTER TABLE usuarios
ADD COLUMN is_admin BOOLEAN NOT NULL DEFAULT 0;

Nao fiz no note:
ALTER TABLE usuarios ADD COLUMN foto_perfil VARCHAR(255) DEFAULT 'usuario.png';

-- UPDATE usuarios
-- SET is_admin = 1
-- WHERE id_usuario = <id_do_usuario>;

UPDATE usuarios
SET is_admin = 1
WHERE id_usuario = 26;

SELECT * FROM produtos;

SELECT * FROM usuarios;